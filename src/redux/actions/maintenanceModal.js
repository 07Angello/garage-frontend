import { types } from "../types/types";

export const maintenanceOpenModal = () => ({ type: types.maintenanceOpenModal });
export const maintenanceCloseModal = () => ({ type: types.maintenanceCloseModal });
